#!/bin/sh
# wait-for-mysql.sh

set -e
#>&2 
echo "Waiting for MySQL..."

#HOST="$1"
#USER="$2"
#PASSWORD="$3"

until echo '\q' | mysql -hdb -uroot -proot; do
#>&2 
    echo "MySQL is unavailable - sleeping"
    sleep 1
done

#>&2   
echo "MySQL is up - end of wait-for-mysql.sh"
